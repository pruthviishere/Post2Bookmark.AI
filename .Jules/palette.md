## 2024-05-22 - [Raw HTML in Extension Popups]
**Learning:** Chrome extensions often start as raw HTML fragments (`div` only) without standard document structure (`html`, `head`, `body`), leading to inconsistent styling and accessibility gaps.
**Action:** Always verify and wrap extension popups in standard HTML boilerplate to ensure reliable CSS rendering and accessibility support.
